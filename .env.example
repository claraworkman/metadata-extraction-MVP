# Azure OpenAI Configuration
AZURE_OPENAI_ENDPOINT=https://your-openai-endpoint.cognitiveservices.azure.com/
AZURE_OPENAI_DEPLOYMENT=gpt-4o

# Azure Document Intelligence Configuration (for PDF OCR)
DOCUMENT_INTELLIGENCE_ENDPOINT=https://your-docintel-endpoint.cognitiveservices.azure.com/

# Azure Blob Storage Configuration
STORAGE_ACCOUNT_NAME=yourstorageaccount

# Optional: Blob Storage Configuration
MARKDOWN_CONTAINER=extracted-markdown
SAVE_MARKDOWN=true

# Parallel Processing Configuration
# IMPORTANT: For large batches (1000+ contracts), reduce MAX_WORKERS to 3 to avoid rate limits
MAX_WORKERS=3          # Number of concurrent workers (default: 10, recommended: 3 for large batches)
MAX_RETRIES=3          # Max retry attempts per file (default: 3)
RETRY_DELAY=2          # Seconds between retries (default: 2)

